version: "3.9"

services:

  backend:

    build: ./backend

    container_name: techspark-backend

    ports:

      - "5000:5000"

    volumes:

      - ./backend:/app

      - ./temp:/temp

      - /var/run/docker.sock:/var/run/docker.sock

    stdin_open: true

    tty: true